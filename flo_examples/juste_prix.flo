entier target = aleatoire(100);
entier guess = lire();

tantque (target != guess) {
  si (guess < target) {
    ecrire(1);
  }

  si (guess > target) {
    ecrire(-1);
  }

  guess = lire();
}

ecrire(0);
